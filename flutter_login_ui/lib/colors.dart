import 'package:flutter/material.dart';

//defining the colors as constants to be used across the app
// to make a color from hexadecimal Value == use color code without #
// Color(0xFF......)

const bgColor = Color(0xFF202020);
const primaryColor = Color(0xFFFFBD73);
